endpoints:
  chatter:
    base_url: ${SURVEY_CHATTER_ENDPOINT_URL}
    model: ${SURVEY_CHATTER_MODEL}
    roles:
      - designer
      - interviewer
  scientist:
    base_url: ${SURVEY_SCIENTIST_ENDPOINT_URL}
    model: ${SURVEY_SCIENTIST_MODEL}
    roles:
      - validator
      - analyst
      - embeddings
